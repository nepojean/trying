#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n, m, k;
    cin >> n >> m >> k;

    vector<int> appartments(n);
    for (int i = 0; i < n; i++) {
        cin >> appartments[i];
    }
    vector<int> available(m);
    for (int i = 0; i < m; i++) {
        cin >> available[i];
    }

    


}